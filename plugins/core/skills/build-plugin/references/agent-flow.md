---
name: agent-flow
description: Compatibility stub. Use `plugins/core/commands/references/agent-flow.md` as the canonical parallel flow.
---

# agent-flow

This legacy location is kept only for compatibility.

Use the canonical flow at:

`../../commands/references/agent-flow.md`
